<article class="post">
    <div class="title">
        <h2>{{title}}</h2>
        <p>{{content}}</p>
    </div>
    <div class="meta">
        by {{user.username}} on {{created_at}}
        |
        <a href="/post/{{id}}">{{comments.length}} comments</a>
    </div>
</article>